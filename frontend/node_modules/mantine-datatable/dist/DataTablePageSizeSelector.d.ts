import { type MantineColor, type MantineSize } from '@mantine/core';
type DataTablePageSizeSelectorProps = {
    size: MantineSize;
    label: string;
    values: number[];
    value: number;
    onChange: (value: number) => void;
    color?: MantineColor;
};
export default function DataTablePageSizeSelector({ size, label, values, value, onChange, color, }: DataTablePageSizeSelectorProps): JSX.Element;
export {};
//# sourceMappingURL=DataTablePageSizeSelector.d.ts.map