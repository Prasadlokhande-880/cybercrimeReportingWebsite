import { type MantineNumberSize, type MantineShadow } from '@mantine/core';
import type { ReactNode } from 'react';
type DataTableRowMenuProps = {
    borderRadius: MantineNumberSize | undefined;
    shadow: MantineShadow | undefined;
    zIndex: number | undefined;
    y: number;
    x: number;
    children: ReactNode;
    onDestroy: () => void;
};
export default function DataTableRowMenu({ borderRadius, shadow, zIndex, y: desiredY, x: desiredX, onDestroy, children, }: DataTableRowMenuProps): JSX.Element;
export {};
//# sourceMappingURL=DataTableRowMenu.d.ts.map